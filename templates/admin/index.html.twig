{% extends('base.html.twig') %}

{% block title %}
    {{ 'admin.index.title'|trans }} - {{ parent() }}
{% endblock %}

{% block body %}
    {{
        include('admin/booking-table.html.twig', {
            'bookings': houseBookings,
            'title': 'house.title',
        })
    }}
    {{
        include('admin/booking-table.html.twig', {
            'bookings': apartmentBookings,
            'title': 'apartment.title',
        })
    }}

    <a href="{{ url('admin_new_booking') }}">{{ 'admin.newBooking.title'|trans }}</a>
{% endblock %}