{% extends 'base.html.twig' %}

{% block title %}
    {{ 'booking.title'|trans }} - {{ parent() }}
{% endblock %}

{% block body %}
    <main>
        <h1>{{ 'booking.title'|trans }}</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Buchungsanfrage stellen</button>
        {{ form_end(form) }}
    </main>
{% endblock %}
